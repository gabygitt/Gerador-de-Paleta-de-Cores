# Gerador de Paleta de Cores (🎨 ColorPalette Studio)

O ColorPalette Studio é uma aplicação web para geração, personalização e gerenciamento de paletas de cores de forma simples, intuitiva e interativa. O projeto foi desenvolvido com foco em praticidade para designers, desenvolvedores e criadores que precisam explorar combinações harmônicas de cores para seus projetos.

A aplicação permite gerar, trancar, salvar e excluir paletas dinâmicas utilizando dois estilos principais: cores totalmente aleatórias ou cores análogas (baseadas em proximidade no círculo cromático). A geração é feita a partir do modelo HSL, com conversão automática para o formato HEX.
